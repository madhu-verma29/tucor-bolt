services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: tucor_dev
      POSTGRES_USER: uco_user
      POSTGRES_PASSWORD: uco_user_password
    ports: ["5433:5432"]
    volumes: ["tucor_pg:/var/lib/postgresql/data"]
volumes:
  tucor_pg:
