package in.tucor.api.auth; import org.springframework.data.jpa.repository.JpaRepository; import java.util.UUID; public interface RegistrationProfileRepository extends JpaRepository<RegistrationProfile,UUID>{}