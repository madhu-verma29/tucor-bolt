'use client'; import {Suspense,useEffect,useState} from 'react';import {useSearchParams} from 'next/navigation';import Link from 'next/link';import {authApi} from '@/lib/auth-api';
function VerifyEmailContent(){const p=useSearchParams();const [s,setS]=useState('Verifying your email...');useEffect(()=>{const t=p.get('token');if(!t){setS('Invalid verification link.');return;}authApi.verifyEmail(t).then(()=>setS('Email verified successfully. You can now sign in.')).catch(e=>setS(e instanceof Error?e.message:'Verification failed.'));},[p]);return <div className="min-h-screen flex items-center justify-center bg-background px-6"><div className="card p-8 w-full max-w-md text-center"><h1 className="text-2xl font-bold text-foreground mb-3">Email Verification</h1><p className="text-sm text-muted-foreground mb-6">{s}</p><Link href="/sign-up-login" className="btn-primary w-full justify-center">Go to Sign In</Link></div></div>}
export default function VerifyEmailPage(){return <Suspense fallback={null}><VerifyEmailContent/></Suspense>}
